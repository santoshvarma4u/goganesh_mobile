import * as React from 'react';
import FGImage from '../../Modules/Common/FGImage';
import images from '../../Theams/Images';

const UpiLogo = props => <FGImage {...props} source={images.upi} />;

export default UpiLogo;
