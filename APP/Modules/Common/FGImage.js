/* A way to import a library and then use it in the code.
    any implications or consequences of this can be handled in the library file itself.
*/
import FastImage from 'react-native-fast-image';
const FGImage = FastImage;
export default FGImage;
